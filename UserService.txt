package progetto.service;

import java.util.List;

import com.fasterxml.jackson.databind.node.ObjectNode;

import progetto.model.Serie;
import progetto.model.SerieSalvata;
import progetto.model.User;

public interface UserService {
	
	ObjectNode userRegistration(User user);
	User getUserByToken(String token);
	ObjectNode updateUserData(User user, String token);
	ObjectNode deleteUser(String token);
	List<User> getUsers();
	ObjectNode userLoginCheck(User user);
	ObjectNode userLogout(String token);
	ObjectNode saveFavorite(SerieSalvata serie);
	ObjectNode updateSerieData(SerieSalvata serie, String id);
	SerieSalvata getSerieByUserId(int userid);

}
